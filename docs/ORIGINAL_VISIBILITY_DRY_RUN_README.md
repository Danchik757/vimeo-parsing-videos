# Original Visibility Dry Run

Separate worker-based dry run for measuring:

- how many Vimeo URLs expose any download options in a logged-in session
- how many of those expose `Original`
- what failure modes appear during an authenticated run

This flow does **not** download media files.
